#!/usr/bin/env bash
set -e -o pipefail

basedir="$(dirname "$BASH_SOURCE")"
cd "$basedir"
. activate

echo ===== Unit Tests
pytest -q bin/cpd.py

echo ===== Integration Tests
. dot/bashrc.inb4
PATH="${basedir}/bin:$PATH"
cpd
